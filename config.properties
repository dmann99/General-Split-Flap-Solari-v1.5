# Split Flap Display Configuration

# Number of rows (lines) to display
rows=15

# Number of columns (characters per line)
columns=50

# Data file path
dataFile=output.json

# Update interval in milliseconds
updateInterval=55000
